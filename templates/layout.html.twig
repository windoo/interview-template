{% extends 'base.html.twig' %}


{% block body %}
    <header>
        <nav class="navbar navbar-expand-xl navbar-light bg-light">
                <div class="container-fluid">
                    <div class="logo">
                        <a class="navbar-brand" href="{{path('index')}}">
                            <img class="logo" src="{{ asset('image/logo.png') }}" 
                            alt="logo commerce ensemble">
                        </a>
                    </div>

                    <div>                
                        <h1>Boite à idées</h1>
                        <p>Partages et donnes ton avis</p>
                    </div>

                    <ul class="navbar-nav">
                        {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_promotAdmin', {id: app.user.id})}}">Administrateur</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href=href="{{path('app_logout') }}" >Déconnexion</a>
                        </li>
                        {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_register')}}">Inscription</a>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </nav>
        
    </header>

    {% block content %}
    
    {% endblock %}
    <footer>
        <ul>
            <li>Your controller at <code><a href="{{ '/home/florent/dev/interview-template/src/Controller/HomepageController.php'|file_link(0) }}">src/Controller/HomepageController.php</a></code></li>
            <li>Your template at <code><a href="{{ '/home/florent/dev/interview-template/templates/homepage/index.html.twig'|file_link(0) }}">templates/homepage/index.html.twig</a></code></li>
        </ul>
    </footer>
{% endblock %}