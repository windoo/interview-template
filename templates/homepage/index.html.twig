{% extends 'layout.html.twig' %}

{% block title %}Boite à idées{% endblock %}

{% block content %}
    {# <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style> #}

       <section>
       <h2>Nouvelle idées</h2>
       {{ form_start(form)}}
        {{form_row(form.proposal, {'attr':{'class': 'form-control'}} ) }}
        <button class="btn btn-primary">Envoyer à la boite à idées</button>
       {{ form_end(form)}}
       </section>

       <section>
       <h2>Donne ton avis sur ces idées</h2>
       {% for proposal in all %}
        <div class="card">
            <div class="card-body">
            <p class="card-title">{{ proposal.proposal }}</p>
            <p>Proposé par  le {{ proposal.createAt |  date("d/m/Y \\à H\\hi") }}</p>
            </div>
            <div>
                <a href="">Pour 👍</a> | <a href="">Contre 👎</a>
                {% if is_granted("ROLE_ADMIN") %}
                       | <a href="{{path('delete_idea', {id: proposal.id})}}" >❌ Suppression</a>
                {% endif %}
            </div>
        </div>
       {% endfor %}
       </section>

{% endblock %}
